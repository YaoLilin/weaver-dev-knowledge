# E10 开发规范

常见的开发规范就不说了，谁要是命名再用拼音简写就拉出去枪毙。

## 前端开发规范

- 任何通过二开在页面上添加元素的组件（比如弹窗、按钮、表格等），都必需在组件的样式名上注明二开标记，如：“ecode 应用id，代码入口（是ecode还是 表单js）”，方便定位二开代码编写位置。（我经常碰到这个按钮不知道是在哪里开发添加的，找的很痛苦）

示例：
appid-${appId} 为ecode 应用id，seconddev-ecode 表示这是在 ecode 二开添加的

```javascript
<Button onClick={handleClick} className={'appid-${appId} seconddev-ecode'} >下载为PDF</Button>
```

