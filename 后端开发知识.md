## 公共类库已上传 Github 👏

本人开发的公共类库，包含工具类以及通用组件，你可以用在你的项目上，欢迎大家提交代码，完善此类库。

github 地址：[YaoLilin/weaver-e9-common: 泛微E9公共类库，包含工具类、通用组件，可以在任何 E9 项目中使用，为开发提供便利](https://github.com/YaoLilin/weaver-e9-common/tree/main#)

gitee : [https://gitee.com/yaolilin/weaver-e9-common](https://gitee.com/yaolilin/weaver-e9-common)

## 开发资料链接集合

[[二次开发] e9 开发资料链接](note://WEBff762f10ead719d2ea6778555bbe38db)

## 开发准则

一些建议的规范，根据本人多年开发踩过的坑总结的一些经验

### 异常处理

#### 避免将异常抛出到上层标准类

例如下面的代码，如果 execute() 方法内不手动捕获异常，如果异常抛出到上层的标准类，则异常日志将不会打印到集成日志，而是打印到 ecology 日志，打印到 ecology 日志有个弊端，就是文件很大，不利于查找日志，我建议将所有二开日志打印到集成日志，另外在上层类中不确定异常是如何处理，可能不会在日志中打印异常栈，导致错误难以查找。

❌ 没有捕获异常，异常将抛出到上层

```java
public class InterfaceParamMappingDetailCmd extends AbstractCommand<InterfaceParamMappingDetail> {  
    @Override  
    public InterfaceParamMappingDetail execute(CommandContext commandContext) {  
        if (id == null || id <= 0) {  
            return null;  
        }  
        InterfaceParamMappingDetail detail = mappingDao.queryDetailMain(id);  
        if (detail == null || detail.getId() == null) {  
            return detail;  
        }  
        List<InterfaceParamMappingDetail.ParamMappingItem> mappings = buildMappings(detail);  
        detail.setMappings(mappings);  
        return detail;  
    }  
}
```


✅ 手动捕获异常并打印
```java
public class InterfaceParamMappingDetailCmd extends AbstractCommand<InterfaceParamMappingDetail> {  
    @Override  
    public InterfaceParamMappingDetail execute(CommandContext commandContext) {  
        try {  
            if (id == null || id <= 0) {  
                return null;  
            }  
            InterfaceParamMappingDetail detail = mappingDao.queryDetailMain(id);  
            if (detail == null || detail.getId() == null) {  
                return detail;  
            }  
            List<InterfaceParamMappingDetail.ParamMappingItem> mappings = buildMappings(detail);  
            detail.setMappings(mappings);  
            return detail;  
        } catch (Exception e) {  
            log.error("执行发生异常", e);  
            throw  e;  
        }  
    }  
}
```


对于其它地方也是如此，比如 WEB 接口类中的方法、流程 Action 类、计划任务等

WEB 接口类中的方法手动捕获异常：

（注：WebExceptionHandler.handle() 内已记录异常信息）

```java
    @Path("handle")
    @POST
    @Consumes(MediaType.APPLICATION_JSON)
    @Produces(MediaType.APPLICATION_JSON)
    public Response handle(@Context HttpServletRequest request,@Context HttpServletResponse response) {
        Map<String, Object> params = ParamUtil.requestJson2Map(request);
        try {
            Map<String, Object> result = getService().handle(params);
            return Response.ok(result).build();
        } catch (Exception e) {
            return WebExceptionHandler.handle(e);
        }
    }
```

#### 不要直接打印异常

不要直接使用 e.printStackTrace() 打印异常，这样异常不会记录到集成日志，请将异常信息记录到日志

❌ 错误示例：

```java
try{
    
}catch(Exception e){
    e.printStackTrace();
}
```

✅ 正确示例：

```java
try{
    
}catch(Exception e){
    // 将异常打印到集成日志
    log.error("执行发生异常",e);
    // 对异常进行其它处理
}
```

### RecordSet 的使用

#### 不要忽略 execute() 方法的执行结果

以及类似的方法：executeQuery() 、executeUpdate() 等。

这些方法的返回结果可以判断 sql 是否执行正确，如果忽略返回结果就不知道 sql 是否执行正确了，如果 sql 执行错误了但没有进行处理，继续执行后续的业务代码将会导致问题影响进一步扩大，难以排查原因。

❌ 不对返回结果进行判断，忽略结果

```java
recordSet.executeQuery(sql, params.toArray())
```

✅ 判断返回结果，如果 SQL 执行失败则进行处理，例如抛出异常

```java
if (!recordSet.executeQuery(sql, params.toArray())) {
    throw new SqlExecuteException("执行sql失败，sql: " + sql + "，params: ["
            + formId + ", " + fieldDbName + ", " + detailTableName + "]", sql);
}
```

#### 使用 getString()获取数值类型

`getInt()` 、`getDouble()`、`getFloat()` 等方法会将字段值转换为指定的类型，因为返回的是基本类型，如果字段值为空将返回意想不到的值，例如 `getInt()` 将返回 -1 ，如果你没有对这样情况的返回结果进行判断，则错误的结果将会导致后面的业务出现错误，最好是先使用 `getString()` 获取字符串值，判断是否为空，然后再转换成指定类型。

可以使用 HuTool 中的 Convert 类转换成指定类型，如果转换失败它会返回 null ，也就是字段值为空的时候返回 null ，这样就知道这个字段是不是空的了

`Convert.toInt(recordSet.getString("parent_id"))`

### 日志处理

- 统一将二次开发日志输出到集成日志，不要将日志输出到 ecology 日志，ecology 日志很大日志不好找
- 捕获异常时，将异常打印到日志，这样可以在日志中看到异常栈，不可以只打印异常信息，例如：

```java
try{
    
}catch(Exception e){
    // 将异常打印到集成日志
    log.error("执行发生异常",e);
    // 对异常进行其它处理
}
```
## 二次开发入门

### 开发环境搭建

[[二次开发] e9 后端开发环境搭建](note://WEB7984e34be904f37a44cd6d9b61c8d2db)

### 包路径规范

建议所有开发文件包路径都在 secdev 包之下，区分二开文件与标准文件，例如：`com.api.interfaces.secdev.extend.web`

在以下带有 secdev 的路径中，secdev 是建议的包路径。

WEB 接口发布包路径：com.api.interfaces.secdev.*

WEB 接口业务包路径：com.engine.interfaces.secdev.*

其它开发包路径：com.customization.secdev.*

### 无侵入二次开发

[[二次开发] e9 后端无侵入二次开发](note://WEB20dca2ef746278e83ca0c0e1ab19fb55)

### 数据库操作

#### RecordSet 数据库操作类

代码中的数据库操作使用 RecordSet 类执行，使用时需要创建 RecordSet 对象

```java
RecordSet recordSet = new RecordSet();
```

进行查询操作使用 `executeQuery(sql,params) `方法，该方法返回布尔值，如果执行不成功则返回 false，其中 params 参数是 sql 语句中的占位符(?)参数值，sql 执行时就会将参数的值替换到占位符中，为防止 sql 注入，如无特殊情况都使用占位符进行注入，不得使用字符串连接。

```java
boolean success = recordSet.executeQuery("SELECT ID FROM table WHERE name=? AND age=?",name,age);
if(!success){
    // sql 查询错误
    log.error("sql 查询错误");
}
// 查询操作一定要调用 next 方法，不然获取不到查询结果
recordSet.next();
recordSet.getInt("ID");
// 获取字段时可转换为其它类型
// recordSet.getString("ID");
```

如果需要查询多条数据，则使用 while 循环，`recordSet.next()` 方法可指定下一行查询数据，如果没有下一行则返回 false

```java
recordSet.executeQuery("SELECT ID FROM table WHERE name=? AND age=?",name,age);
while (recordSet.next()) {
    int id = recordSet.getInt("ID");
}
```

数据库修改操作使用` executeUpdate(sql,params) `方法执行，update ,insert,delete 操作都可以使用此方法，如果执行失败则返回 false

```java
boolean success = recordSet.executeQuery("UPDATE name=?,age=? FROM table WHERE id=?",name,age,id);
if(!success){
    // 更新失败
    log.error("更新失败");
}
```

`execute(sql)` 也能执行 sql 查询和修改，但不建议使用，不能使用占位符，操作类型不明确，不能通过方法名确定是查询还是修改。

RecordSet 对数据库进行操作时不会在当前代码块抛出异常，如果 sql 执行不成功方法会返回 false。

##### 字段值获取方式

RecordSet 获取查询的字段值可以有以下方式

通过字段名获取，这种是最常用的

```java
recordSet.getString("name");
```

通过列位置获取，比如查询有两个字段，第一个字段的列位置就是1，第二个字段的列位置是2，列位置是从1开始的

```java
recordSet.getString(1)
```

##### 数字类型字段值获取问题

如果使用 `recordSet.getInt("field")` 或`recordSet.getDouble("field")`获取数字类型的字段值，如果字段值为空则获取到的值可能为 -1 ，在代码中需要注意判断，或者可以先使用 `recordSet.getString("field")`获取到字符串值，判断是否为空，不为空才转为数字类型。

##### 批量操作

##### 相关问题

RecordSet 批量执行报 java.util.ArrayList cannot be cast to java.lang.Stri：

List中的参数个数与sql语句的占位符数量不一致导致

#### RecordSetDataSource 使用第三方数据源进行数据库操作

构造方法中传入第三方数据源标识，该标识可在系统后端页面集成中心中找到。

```java
private RecordSetDataSource rst = new RecordSetDataSource("E8OA");
```

#### RecordSetTrans 数据库操作类（事务）

使用该类可执行事务

主要方法：

`setAutoCommit()`是否自动提交事务，如要要事务回滚，需要设为false

`commit()` 提交事务，在执行更新或插入操作后，可提交事务，使更改生效

`rollback()` 回滚事务，在数据库执行失败（如返回 false 或抛出异常）可执行回滚操作，撤回之前进行的插入或更新。

#### RecordSetExecutionInterface 数据库操作接口

`RecordSet` 和 `RecordSetTrans` 这些数据库操作类都实现了 `RecordSetExecutionInterface` 接口，因此在方法中使用 `RecordSetExecutionInterface` 接口而不是具体的实现类对于事务执行很有帮助，如果不需要执行事务 ，则传入 `RecordSet` ,如果需要执行事务则传入 `RecordSetTrans` 

```java
public static boolean insertWithRsInterface(String tableName, Map<String, Object> fieldData,RecordSetExecutionInterface recordSet) throws Exception {

}
```

使用方法：

`executeSql()` 方法可执行sql，可进行查询或修改（插入），可使用占位符注入参数，参数说明：

- sql：执行sql
- isQuerySql：是否为查询sql，如果是查询传入 true，如果是修改（插入）传入 false
- poolName：我还不知道是什么意思，看名称可能是连接池名称，用于连接外部数据源，如果是本地数据源传入空字符串就好了
- hasNewConn：我也不知道是什么意思，传入 true 或 false 都可以
- params：sql 中的参数值，如果使用占位符注入参数（占位符:?）需要传入该参数

sql 查询示例：

```java
RecordSetExecutionInterface recordSet = new RecordSet();
if (!recordSet.executeSql(sql, true, "", false, uuid)) {
    logger.error("根据uuid查询数据id失败");
    continue;
}
if (!recordSet.next()) {
    logger.warn("无法查询到数据id，uuid:" + uuid);
    continue;
}
ids.add(recordSet.getInt("id"));
```

sql 插入示例：

```java
public static boolean inserByRsInterface(String tableName, Map<String, Object> fieldData,RecordSetExecutionInterface recordSet) throws Exception {
    String  sql = SqlUtil.buildInsertSql(tableName, new ArrayList<>(fieldData.keySet()));
    List<Object> insertData = new ArrayList<>(fieldData.values());
    logger.info("sql:" + sql);
    return recordSet.executeSql(sql,false,"",false ,insertData.toArray());
}
```

## java配置文件

### 属性获取

#### 使用 BaseBean 获取（不推荐）

需要创建 BaseBean 对象，不推荐。

BaseBean baseBean = new BaseBean(); String propValue = baseBean.getPropValue("testProp", "sms");

#### 使用 Prop 类获取（推荐）

使用 weaver.file.Prop 获取配置文件属性，注意如果配置文件属性值有中文，获取属性值时会导致乱码，原因是没有使用 UTF-8 编码。

// fileName：文件名，不需要带文件扩展名，propName：属性名 String value = Prop.getPropValue(fileName, propName);

属性值有中文时，只能将中文转为 unicode 放到配置文件中

转换地址：[在线 Unicode 编码转换 | 菜鸟工具 (runoob.com)](https://c.runoob.com/front-end/3602/)

#### 使用 Properties 类获取（支持中文）

使用 java.util.Properties 类获取配置属性可解决中文乱码问题，不过这种用法比较复杂，建议在有中文的情况下使用

首先需要获取 Properties对象

/** * 获取配置文件，解决了配置文件中文乱码问题 * * @param propFieldName 配置文件名称，不需要带扩展名后缀 * @return Properties对象 */ public static Properties fetchProperties(String propFieldName) { Properties prop = new Properties(); String path = GCONST.getPropertyPath() + propFieldName + ".properties"; try (InputStream inputStream = Files.newInputStream(Paths.get(path))) { prop.load(new InputStreamReader(inputStream, StandardCharsets.UTF_8)); } catch (IOException e) { log.error("获取配置文件失败", e); } return prop; }

然后就能获取属性值

Properties properties = fetchProperties(fileName); String value = properties.getProperty(propName);

#### 配置类

不建议将获取配置写在业务代码上，这样会导致获取配置太过分散，通常是建一个配置类获取配置，如：

public class SuppApiConfiguration { private static final String FILE_NAME = "FW20241127-suppApi"; private static final int TEN_MINUTE_EXPIRE = 60 * 10; private SuppApiConfiguration() { } public static String getAppId() { return PropertiesUtil.getPropValue(FILE_NAME, "appId", true, true, TEN_MINUTE_EXPIRE); } public static String getKey() { return PropertiesUtil.getPropValue(FILE_NAME, "key", true, true, TEN_MINUTE_EXPIRE); } public static String getServerUrl() { return PropertiesUtil.getPropValue(FILE_NAME, "serverUrl", true, true, TEN_MINUTE_EXPIRE); } public static String getBudgetChangeStatusApiUrl() { return PropertiesUtil.getPropValue(FILE_NAME, "budgetChangeStatusApiUrl", true, true, TEN_MINUTE_EXPIRE); } }

## 打印日志

日志级别默认为 info

二开日志一律打印到 Integration.log ，不得打印到其它日志文件，如果打印到 ecology.log ，由于该日志太过庞大会导致日志查找困难。

创建日志对象

weaver.integration.logging.Logger log = weaver.integration.logging.LoggerFactory.getLogger(this.getClass());

打印日志

log.info("日志内容");

可将异常打印到日志

try { Map<String, Object> result = getService().handle(params); return Response.ok(result).build(); } catch (Exception e) { log.error("错误", e); return WebExceptionHandler.handle(e); }

## 缓存

[[二次开发] 使用缓存](note://WEBd69024e228a646ee776559ba1a8eba47)

参考：[泛微在线文档](https://e-cloudstore.com/doc.html?appId=da5134c3c57f4c6f87815cf1473f674f#4.%20%E7%BC%93%E5%AD%98%E7%9B%B8%E5%85%B3)

### 判断是否使用 Redis

当系统集成 Redis 时，Util_Redis.getInstance() 不会为 null

/** * 判断是否使用redis * * @return 是否使用redis */ public static boolean isRedis() { return Util_Redis.getInstance() != null; }

## 常用数据库表

- workflow_requestbase ：流程请求表，记录每次发起流程的流程信息，里面可获取创建人、创建时间、当前节点、是否归档等信息
- workflow_base：流程基本信息表，记录流程的基本信息和一些设置，包括流程名称、流程类型、表单id等
- workflow_currentoperator：工作流请求节点操作人信息表，可获取节点操作人的操作信息，如操作类型、操作时间、是否超时等。
- workflow_requestlog：工作流请求签字日志表，可获取流程流转的节点签字意见、操作时间、提交类型、下一个节点id等。
- docdetail：知识模块文档信息表，可查看文档的标题、创建人、所属目录id等。
- hrmresource：人力资源表，可查看组织架构中的人员信息。
- hrmdepartment：人力资源部门表，可查看部门信息
- hrmsubcompany：人力资源分部表，卡查看分部信息

## 开发规范

[[二次开发] 开发规范](note://WEBbe6967d66ad909da630e088708a12cc8)

# WEB 接口开发

[[二次开发] e9 后端接口开发](note://WEBe4df18e0354760adda98a5cafd8ca7ea)

# WeaverBoot 依赖注入

Weaverboot-E9 提供一套完整的 IOC + AOP 的解决方案。可以很快速的代理所有的接口。同时提供类似与spring 的依赖注入的写法。方便快速编码。

我不推荐使用 Weaverboot 进行依赖注入，可能会有在容器中找不到 bean 的情况，有一次我在实现类上加了@WeaIocService 注解，可却在日志中报错提示找不到接口的实现类。并且已经在官方找不到 Weaverboot 的文档。

参考：[泛微在线文档](https://e-cloudstore.com/doc.html?appId=da5134c3c57f4c6f87815cf1473f674f#3.5.1%20%E4%BD%BF%E7%94%A8%20Weaverboot-E9%EF%BC%88beta%EF%BC%89)